# 引导：持仓快照与收益分析项目

当你把本项目拉到新设备后用 WorkBuddy 打开，请让 WorkBuddy 阅读本文件。WorkBuddy 将根据以下指引完成配置，或列出需要你手动操作的步骤。

---

## 项目简介

每日自动拉取实时行情，计算持仓收益，生成：
- `portfolio_snapshots/YYYY-MM-DD.json` — 每日持仓快照
- `report.html` — 自包含收益分析可视化报告
- CloudStudio 公网链接 — 随处可访问的报告

**股价来源**：通过 westock-mcp 连接器拉取 A 股/港股/美股实时行情，不依赖腾讯文档的 `=STOCK()` 公式。

---

## 文件结构

```
├── snapshot_live.py            # 【核心】CSV + 实时行情 → JSON 快照
├── generate_report.py          # JSON 快照 → HTML 报告生成器
├── import_historical.py        # 历史截图数据导入工具
├── config.json                 # 页面模块显隐配置文件
├── run_daily.py                # 旧版入口（不再使用）
├── portfolio_snapshot.py       # 旧版 CSV 解析器（不再使用）
├── 引导.md                     # 本文件
├── MIGRATION.md                # 跨设备迁移详细文档
├── portfolio_snapshots/        # 历史快照（12+ 份，2026-01-01 → 最新）
└── .gitignore                  # Git 忽略配置
```

**脚本均为纯 Python 标准库，无需 pip 安装任何依赖。**

---

## 前置条件

### 需要的连接器

| 连接器 | 用途 |
|--------|------|
| **腾讯文档** | 读取持仓表（股票代码、成本、数量、汇总数据） |
| **westock-mcp（腾讯自选股）** | 拉取 A 股/港股/美股实时行情 |
| **CloudStudio**（内置） | 部署报告到公网 |

### 腾讯文档中的代码列

持仓表需要在「公司名称」后面有一列「代码」，填入股票交易代码：

| 公司名称 | 代码 |
|---------|------|
| 海螺水泥 | 600585.SH |
| 康方生物 | 09926.HK |
| 亚盛医药 | 06855.HK |
| 传奇生物 | LEGN.US |
| … | … |

格式：A 股 `代码.SH` / `代码.SZ`，港股 `代码.HK`，美股 `TICKER.US`。

---

## 新设备配置流程

每一步标注「自动」表示 WorkBuddy 可直接执行，「手动」表示需要你参与。

### 1. 连接连接器（手动）

在 WorkBuddy 左侧「连接器」面板，依次连接：
1. **腾讯文档** — 登录与持仓文档相同的腾讯账号
2. **westock-mcp（腾讯自选股）** — 登录自选股账号（免费）

> 验证：连好后，让我用 `search_file 持仓` 搜索腾讯文档（应找到 `fGemVXqsvRGM`），再用 `data_quote` 查一只股票测试 westock。

### 2. 确认 Python 路径（自动）

WorkBuddy 托管 Python 路径（当前设备）：
- Windows: `C:\Users\<用户名>\.workbuddy\binaries\python\versions\3.13.12\python.exe`
- Mac/Linux: `~/.workbuddy/binaries/python/versions/3.13.12/python.exe`

让 WorkBuddy 确认路径，记为 `{PYTHON}`。项目目录绝对路径记为 `{PROJECT}`。

### 3. 创建每日自动化任务（自动）

让 WorkBuddy 使用 `automation_update` 创建任务：

| 参数 | 值 |
|------|-----|
| name | 每日持仓快照（交易日） |
| scheduleType | recurring |
| rrule | FREQ=WEEKLY;BYDAY=MO,TU,WE,TH,FR;BYHOUR=23;BYMINUTE=55 |
| cwds | {PROJECT} |
| status | ACTIVE |

提示词：
```
执行每日持仓快照并部署公网报告，步骤如下：

## 第0步：读取腾讯文档持仓表
使用 mcp__tencent-docs__sheet.get_cell_data 读取全部数据，参数：
- file_id: "fGemVXqsvRGM"
- sheet_id: "000001"
- start_row: 0, end_row: 163
- start_col: 0, end_col: 14
- return_csv: true
从返回结果中提取 csv_data 字段。

## 第1步：保存 CSV 到临时文件
将 csv_data 写入：{PROJECT}/.tmp_csv.csv

## 第2步：动态提取代码并获取实时行情
从 CSV 数据的「代码」列（第4列，索引3）提取所有非空值，筛选以 .SH/.SZ/.HK/.US 结尾的条目。
转换为 westock 格式（去掉后缀，前面加 sh/sz/hk/us，美股大写），逗号拼接后调用 mcp__westock-mcp__data_quote

## 第3步：组装价格 JSON
从 westock 返回的 price 字段，转回 CSV 格式的 key：
sh600585 → 600585.SH, hk09926 → 09926.HK, usPDD → PDD.US
写入：{PROJECT}/.tmp_prices.json

## 第4步：生成持仓快照
{PYTHON} {PROJECT}/snapshot_live.py {PROJECT}/.tmp_csv.csv {PROJECT}/.tmp_prices.json

## 第5步：生成收益分析报告 HTML
{PYTHON} {PROJECT}/generate_report.py

## 第6步：部署到 CloudStudio 公网
copy "{PROJECT}/report.html" "{PROJECT}/deploy/index.html"
调用 workbuddy_cloudstudio_deploy，directory = "{PROJECT}/deploy"

## 第7步：报告结果
- 持仓数量、总收益、总收益率
- 公网链接：以 deploy 返回的 shareLink 为准

## 异常处理
- 任何步骤失败 → 报告错误，不生成空文件
- 当天 JSON 已存在 → 覆盖更新
- 完成后清理 {PROJECT}/.tmp_csv.csv, {PROJECT}/.tmp_prices.json
```

> 调度时间：每个交易日（周一至周五）16:30（A股收盘后）。可调整。

### 4. 首次运行验证（手动 / 自动）

让 WorkBuddy 手动触发一次自动化任务，验证全流程通顺。
以 deploy 返回的 shareLink 为准保存公网链接（`app.codebuddy.work` 域名有 CDN 缓存，用 e2b 直连链接即时生效）。

---

## 报告图表说明

| 区块 | 内容 |
|------|------|
| 01 概览 | 总当前值、总收益、总收益率、最大回撤 |
| 02 综合走势 | 资产走势、收益走势、收益率（双轴折线，点击图例显隐） |
| 03 回撤分析 | 整体回撤（红色面积填充） |
| 04 仓位率 | 持仓占当前总市值百分比 |
| 05 结构分布 | 仓位分布 + 市场分布 环形图（大小一致、legend 在底部） |
| 06 收益排行 | 各标的收益条形图（红涨绿跌） |
| 07 统计摘要 | 4 列网格，四个股票极值指标在同一行 |
| 08 持仓明细 | 完整持仓表（公司名称居左） |
| 09 盈亏日历 | 日/月/年三种视图热力图，含盈亏额+收益率 |
| 10 清仓盈亏 | 已卖出标的的盈亏柱状图（退出市值 − 首次投入） |
| 右上角按钮 | SAVE AS PNG — 截图保存整页为图片 |

配色：黑白红 Swiss Modernism 2.0，中国惯例（涨=红，跌=绿）。
移动端已优化（600px 断点），禁止文字选中，图例 hover 显示手型光标。

---

## 常见问题

**Q: 历史快照能带走吗？**
A: 能。`portfolio_snapshots/` 随项目一起 git clone，趋势图/回撤自动带上完整历史。

**Q: 新设备需要装 Python 依赖吗？**
A: 不需要。所有脚本只用 Python 标准库。

**Q: 为什么不用文档里的 =STOCK() 公式？**
A: MCP 读取不触发公式重新计算。改用 westock-mcp 直接拉实时行情，更可靠。

**Q: CloudStudio 链接会变吗？**
A: 同一个 sandboxId 通常不变。但 app.codebuddy.work 有 CDN 缓存，用 e2b 直连链接即时生效。以每次 deploy 返回的 shareLink 为准。

**Q: 怎么添加更多历史截图数据？**
A: 编辑 `import_historical.py`，参考已有格式添加 `build_snapshot` 调用，然后运行该脚本并重新生成报告。

**Q: 怎么控制页面显示哪些模块？**
A: 编辑项目根目录 `config.json`，将不想显示的模块设为 `false`。重新生成报告后生效。

**Q: 怎么增减持仓？**
A: 在腾讯文档中增删行、填入代码即可。代码从 CSV 动态提取，自动化无需修改。
