# 引导：持仓快照与收益分析项目

当你把本项目拉到新设备后用 WorkBuddy 打开，请让 WorkBuddy 阅读本文件。WorkBuddy 将根据以下指引完成配置，或列出需要你手动操作的步骤。

---

## 项目简介

每日从腾讯文档「持仓」表自动抓取持仓数据，生成：
- `portfolio_snapshots/YYYY-MM-DD.json` — 每日持仓快照
- `report.html` — 自包含收益分析可视化报告
- CloudStudio 公网链接 — 随处可访问的报告

---

## 文件结构

```
├── portfolio_snapshot.py      # CSV → JSON 快照解析器
├── generate_report.py         # JSON 快照 → HTML 报告生成器
├── import_historical.py       # 历史截图数据导入工具（仅用于一次性的截图转快照）
├── run_daily.py               # 每日更新合并入口
├── 引导.md                    # 本文件
├── MIGRATION.md               # 跨设备迁移详细文档
├── portfolio_snapshots/       # 历史快照（11 份，2026-01-01 → 2026-07-27）
└── .gitignore                 # Git 忽略配置
```

**脚本均为纯 Python 标准库，无需 pip 安装任何依赖。**

---

## 新设备配置流程

请 WorkBuddy 按以下顺序完成配置。每一步标注了「自动」表示 WorkBuddy 可直接执行，「手动」表示需要你参与。

### 1. 连接腾讯文档连接器（手动）

在 WorkBuddy 左侧「连接器」面板找到「腾讯文档」，登录**与持仓文档相同的腾讯账号**。

> 验证：登录后，让我用 `search_file 持仓` 搜索，应能找到 file_id `fGemVXqsvRGM`。

### 2. 确认 Python 路径（自动）

WorkBuddy 托管 Python 路径（当前设备）：
- Windows: `C:\Users\<用户名>\.workbuddy\binaries\python\versions\3.13.12\python.exe`
- Mac/Linux: `~/.workbuddy/binaries/python/versions/3.13.12/python.exe`

让 WorkBuddy 运行以下命令确认路径：
```bash
ls ~/.workbuddy/binaries/python/versions/3.13.12/python.exe
# 或
python3 -c "import sys; print(sys.executable)"
```

记录确认后的 Python 路径，记为 `{PYTHON}`。项目目录绝对路径记为 `{PROJECT}`。

### 3. 创建每日自动化任务（自动）

让 WorkBuddy 使用 `automation_update` 工具创建任务，参数：

| 参数 | 值 |
|------|-----|
| name | 每日持仓快照 |
| scheduleType | recurring |
| rrule | FREQ=WEEKLY;BYDAY=MO,TU,WE,TH,FR;BYHOUR=16;BYMINUTE=10 |
| cwds | {PROJECT} |
| status | ACTIVE |

提示词内容：
```
执行每日持仓快照并部署公网报告。需先替换下方路径值为本机实际值。

Python 路径 = {PYTHON}
项目目录 = {PROJECT}

步骤：
1. 用 mcp__tencent-docs__sheet.get_cell_data 读取持仓表：
   - file_id: "fGemVXqsvRGM"
   - sheet_id: "000001"
   - start_row: 0, end_row: 163
   - start_col: 0, end_col: 12
   - return_csv: true
   提取返回结果中的 csv_data 字段。

2. 将 csv_data 写入临时文件：{PROJECT}/.tmp_portfolio_csv.csv

3. 执行命令：{PYTHON} {PROJECT}/run_daily.py
   （该脚本依次：CSV→JSON 快照 → 生成 report.html → 复制到 deploy/index.html）

4. 调用 workbuddy_cloudstudio_deploy 工具：
   directory = {PROJECT}/deploy
   （部署后记录返回的 shareLink 为最新公网链接）

5. 报告结果：持仓数量、总收益、总收益率（用文档中的值，不是持仓行计算值）。
   附上公网链接：以第4步 workbuddy_cloudstudio_deploy 返回的 shareLink 为准。

异常处理：
- 第1步腾讯文档读取失败 → 报错，不生成空文件
- 当天 JSON 已存在 → 覆盖更新
- 完成后清理 {PROJECT}/.tmp_portfolio_csv.csv
```

> 调度时间：每个交易日（周一至周五）16:10。你可根据需要调整 rrule。

### 4. 首次运行验证（手动 / 自动）

让 WorkBuddy 手动触发一次自动化任务，验证全流程通顺。
如果第 3 步中的 CloudStudio 部署返回了新链接，记录下来替换旧书签。

---

## 手动运行（不依赖自动化）

如果不想设置自动化，也可手动运行：

```bash
# 第1步：让 WorkBuddy 从腾讯文档拉取 CSV 数据
# （使用 mcp__tencent-docs__sheet.get_cell_data）

# 第2步：保存 CSV 到 .tmp_portfolio_csv.csv

# 第3步：一键执行
python run_daily.py
```

---

## 报告图表说明

报告含以下区块，所有图表均支持点击图例显隐对应曲线：

| 区块 | 内容 |
|------|------|
| 01 概览 | 总当前值、总收益、总收益率、最大回撤 |
| 02 综合走势 | 资产走势、收益走势、收益率（双轴折线） |
| 03 回撤分析 | 整体回撤（红色面积填充） |
| 04 仓位率 | 持仓占当前总市值百分比（文档 `holdings_pos_curr_pct` 字段） |
| 05 结构分布 | 仓位分布环形图 + 市场分布环形图 |
| 06 收益排行 | 各标的收益条形图（红涨绿跌） |
| 07 统计摘要 | 收益率极值、收益额极值、回撤等 |
| 08 持仓明细 | 完整持仓表（公司名称居左） |

配色：黑白红 Swiss Modernism 2.0 风格，中国市场惯例（涨=红，跌=绿）。

---

## 常见问题

**Q: 历史快照能带走吗？**
A: 能。`portfolio_snapshots/` 目录下所有 JSON 文件随项目一起 git clone，趋势图/回撤自动带上完整历史。

**Q: 新设备需要装 Python 依赖吗？**
A: 不需要。所有脚本只使用 Python 标准库（csv、json、os、glob、datetime、subprocess）。

**Q: CloudStudio 链接会变吗？**
A: 同一设备同一目录部署 sandboxId 通常不变。但访问 URL 可能随 CloudStudio 调度变化，以每次 `workbuddy_cloudstudio_deploy` 返回的 shareLink 为准。

**Q: 怎么添加更多历史截图数据？**
A: 编辑 `import_historical.py`，参考已有格式添加 `build_snapshot` 调用，然后运行该脚本并重新生成报告。
