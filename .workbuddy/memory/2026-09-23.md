# 2026-09-23 工作日志

## 每日持仓快照（automation）
- 全流程成功执行，生成第 60 份快照 `portfolio_snapshots/2026-09-23.json`，报告已部署（公网链接不变）。
- 数据（行情 2026-09-22 收盘）：持仓投入 228.77万 / 当前 227.27万 / 收益 -1.50万(-0.66%)；总收益 -32.43万 / 总收益率 -12.56%（基数 258.30万）。
- 基准：沪深300 YTD -1.84%、纳斯达克 YTD +17.22%。
- Git commit 9bff75d，push 成功。

## 流程观察
- `data_quote` 批量取 9 码只返回 8 个（漏 `sh000300`），且该码单独重试即限频；改 `data_minute` 读 `qt.sh000300[61]` 拿 YTD 成功。批量调用后**必须核对返回 data 的 keys 是否齐全**。
- 指数 YTD 的合理性交叉验算（上一档 YTD × (1+当日涨跌幅)）本次两个指数均吻合，可作为标准校验步骤。

## 每日持仓快照（16:10 档 automation）
- 全流程成功，生成第 60 份快照（覆盖当日早档）。持仓 8 只 | 投入 228.21万 | 当前 227.47万 | 持仓收益 -0.75万 (-0.33%) | 总收益 -32.23万 / 总收益率 -12.48%（基数 258.30万）。
- 汇率由 6.7112/0.8552 更新为 US 6.6958 / HK 0.8531，导致投入/当前人民币口径较早档略降。
- 基准：沪深300 YTD -2.43%（CAGR -1.88%）、纳斯达克 YTD +17.22%（CAGR +12.66%）。
- 较 09-22 档：持仓收益 -0.87%→-0.33%（+0.54pct）、总收益率 -12.74%→-12.48%（+0.26pct）。
- `data_quote` 本次会话**全程限频**，7 只个股 + 2 只指数全部走 `data_minute` 一次跑通。指数 YTD 下标发生变化：`sh000300` YTD 落在 `[62]`（-2.43），`usIXIC` 仍在 `[54]`（17.22）——**不要死记下标，必须用「上一档 YTD × (1+当日涨跌幅)」反推校验**。
- 部署：`workbuddy_sites_deploy` 仍报"预留域名 portfolio-snapshot-24056 未绑定"（不重试）；curl 验证稳定域名已含 09-23 16:32:21 数据（total_roi -12.48 / total_pnl -322336.16 / holdings_count 8）。
- Git commit 8f6a280，push 成功（9bff75d..8f6a280，一次成功）。
